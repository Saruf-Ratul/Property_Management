# Copy to .env.local for local development.
# In docker-compose the API is reachable at /api via the nginx proxy, so leaving
# this empty (or set to /api) is correct for the production container build.
VITE_API_BASE_URL=/api
